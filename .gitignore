node_modules/
.env
.env.*
!.env.example

# Rendered samples are committed on purpose — they are what a client is shown.
# Nothing else generated belongs in the repo.
*.log
.DS_Store
Thumbs.db
desktop.ini

# the voice agent's python env (2GB of Torch and Whisper weights)
.venv/
__pycache__/
*.pyc

# demo mode writes every email it "sends" here
.sena-demo-mail/

# portable helper binaries (cloudflared) — downloaded, never committed
tools/
